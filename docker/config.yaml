logging:
  level: "info"
  format: "json"
 # output: "watchtower.log"

auth:
  jwt_secret: "1234567890abcdef1234567890abcdef"
  jwt_ttl_hours: 24

database:
  monitoring:
    dsn: "postgresql://monitoring_svc:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  maintenance:
    dsn: "postgresql://maintenance_svc:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  auth:
    dsn: "postgresql://auth_svc:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  metrics:
    dsn: "postgresql://metrics_query_svc:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  healthchecker:
    dsn: "postgresql://healthchecker:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  analyzer:
    dsn: "postgresql://analyzer:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  contacts:
    dsn: "postgresql://alert_contacts_svc:password@watchtower-postgres:5432/watchtower?sslmode=disable"
  migrations:
    dsn: "postgresql://postgres:postgres@watchtower-postgres:5432/watchtower?sslmode=disable"

migrations_dir: "./migrations"

redis:
  url: "redis://watchtower-redis:6379/0"
